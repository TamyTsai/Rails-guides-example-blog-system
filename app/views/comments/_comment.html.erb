<% unless comment.archived? %>
<!-- 類似地，在我們的留言部分檢視（app/views/comments/_comment.html.erb）中，我們將使用 archived? 方法來 避免 顯示任何 已封存 的留言 -->
<!-- archived?為定義在comment model的方法，會回傳 留言資料表中status欄位值是否為archived -->
    <p>
        <strong>留言者：</strong>
        <%= comment.commenter %>
    </p>
    <p>
        <strong>留言：</strong>
        <%= comment.body %>
    </p>

    <p>
        <!-- <%= link_to "刪除留言", article_path(@article), data: {turbo_method: :delete, turbo_confirm: "確定刪除此文章？"} %> -->
        <%= link_to "刪除留言", [comment.article, comment], 
        data: {
            turbo_method: :delete, 
            turbo_confirm: "確定刪除此留言？"
        } %>
        <!-- 按一下這個新的「刪除留言」連結會觸發 DELETE /articles/:article_id/comments/:id 至我們的 CommentsController，然後它可以使用這個連結來尋找我們要刪除的留言 -->
    </p>
<% end %>